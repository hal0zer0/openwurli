# Python virtual environment
.venv/

# AI agent session data (private)
.claude/

# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.idea/
.vscode/

# Rust/Cargo build artifacts
target/
build/
cmake-build-*/
*.o
*.so
*.dylib
*.dll

# Python bytecode and virtual environments
__pycache__/
*.pyc
venv/

# Copyrighted reference materials (obtain separately, see docs/SCHEMATIC_SOURCE.md)
docs/verified_wurlitzer_200A_series_schematic.pdf
*.mid
midi/
midis/

# Rendered schematic tiles (large PNGs, regenerate from PDF as needed)
schematic_tiles/

# External tools (cloned repos, downloaded binaries)
netlistify/
tools/pulse-visualizer/

# Audio output (regenerable)
*.wav
*.flac
tools/reed-renderer/output/

# Local diagnostic baselines / regression dumps (regenerate as needed)
baseline/

# Ad-hoc diagnostic example harnesses (built during debugging sessions,
# not part of the public API — production demos go through preamp-bench
# / reed-renderer crates).
crates/openwurli-dsp/examples/

# Local SPICE testbenches (auxiliary; canonical netlists are in
# spice/subcircuits/ and the spice/testbench/tb_*.cir set)
spice/testbench/tb_power_amp_harmonics.cir

# Local utility scripts
tools/render-video.sh

# Market research (untracked, not part of the project deliverables)
docs/market-research/

# Release codename candidates (personal reference, not project deliverable)
docs/release-codenames.md

# SPICE simulation output (regenerate by running testbenches)
spice/output/
*.dat
*.data
*.plt

# ML pipeline artifacts (regenerate with ml/pipeline.py)
ml/ml_data/
ml/input
ml/notes.json
ml/scored_notes.json
ml/harmonics.json
ml/model_harmonics.json

# Secrets (preemptive)
.env
.env.*
*.pem
*.key

# Misc
desktop.ini
*.log
*.bak
*.orig
